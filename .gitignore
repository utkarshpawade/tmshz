# env
.env
.env.local

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
backend/ml/artifacts/*.pkl
# NOTE: the trained .joblib model artifacts ARE committed on purpose —
# they're needed in the Docker image so the deployed backend can serve
# predictions without retraining on every cold start.

# node / next
node_modules/
.next/
out/
npm-debug.log*
frontend/.env*.local
frontend/tsconfig.tsbuildinfo

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
