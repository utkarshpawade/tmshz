__pycache__/
*.pyc
.env
.venv/
venv/
.pytest_cache/
ml/artifacts/*.pkl
# .joblib model artifacts are intentionally kept — the deployed backend
# loads them at runtime to serve predictions without retraining.
